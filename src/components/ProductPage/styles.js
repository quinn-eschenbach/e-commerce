import { makeStyles } from "@material-ui/core/styles";

export default makeStyles(()=>({
    image:{
        maxHeight: "70vh",
        width: "100%"
    },
    icons:{
        display: "flex",
        flexDirection: "column",
        alignItems: "center"
    }
}))